<?xml version="1.0" encoding="UTF-8"?>
<project name="AgileLearningProject" default="junitgui" basedir=".">
	<property name="junitJar" value="\junit3.8.1\junit.jar" />
	<project name="src.dir" value="${basedir}\source" />
	<project name="build.dir" value="${basedir}\classes" />
	
	<path id="classpath"> 
		<pathelement location="${junitJar}" />
		<pathelement location="${bulid.dir}" />
	</path>
	
	<target name="init" >
		<mkdir dir="${build.dir}" />
	</target>
	
	<target name="bulid" depends="init" description="build all">
		<javac 
			srcdir="${src.dir}" destdir="${build.dir}"
			source="1.7"
			deprecation="on" debug="on" optimize="off" includes="**" >
			<classpath refid="classpath" />
		</javac>
	</target>
	
	<target name="junitgui" depends="build" description="run junitgui">
		<java calssname="junit.awtui.TestRunner" fork="yes">
			<arg value="com.su.testall.AllTests" />
			<classpath refid="classpath" />
		</java>
	</target>
	
	<target name="clean">
		<delete dir="${build.dir}" />
	</target>
	<target name="rebuildAll" depends="clean,build" description="rebuild all" />
</project>
